<Window x:Class="SalesWPFApp.Popup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SalesWPFApp"
        mc:Ignorable="d"
        Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" 
               Padding="5 10"
               FontSize="24"
               FontWeight="Bold"
               Grid.Row="0"/>
        <TextBlock Text="{Binding Message}" Padding="5 10" Grid.Row="1"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 10 0 0" HorizontalAlignment="Right">
            <Button Content="{Binding Button1Title}" Click="Button1_Click"/>
            <Button Content="{Binding Button2Title}" Click="Button2_Click"/>
        </StackPanel>
    </Grid>
</Window>
